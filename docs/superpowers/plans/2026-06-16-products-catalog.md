# products (каталог) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: use superpowers:subagent-driven-development або superpowers:executing-plans. Кроки — checkbox (`- [ ]`).

**Goal:** Апка `products` з моделями Category/Product, каталогом (фільтри/пошук/сорт/пагінація) і детальною сторінкою (інфо), на базі шаблону Hop-and-Barley.

**Architecture:** Тонкі CBV; логіка вибірки — в `get_queryset()` (без django-filter); кастомний `ProductQuerySet.active()`; авто-slug у `save()`. Шаблони успадковують `base.html`, витягнутий зі статичного шаблону. Тести — pytest-django + factory_boy, TDD.

**Tech Stack:** Django 5.2, pytest-django, factory_boy, Pillow, WhiteNoise.

**Запуск тестів:** `docker compose up -d --wait db` (один раз), далі `uv run pytest`. Лінт/типи: `uv run ruff check . && uv run mypy .`.

---

## File Structure

- `products/__init__.py`, `apps.py`, `models.py`, `views.py`, `urls.py`, `admin.py`
- `products/migrations/` — міграції
- `products/management/commands/seed_catalog.py` — seed
- `products/templates/products/{product_list,product_detail}.html`
- `templates/base.html` — переписати зі спільного хедера/футера шаблону
- `static/` — `css/main.css`, `img/`, потрібний `js/`
- `products/tests/{__init__,factories,test_models,test_views,test_seed}.py`
- Modify: `config/settings/base.py` (INSTALLED_APPS), `config/urls.py` (include), `pyproject.toml` (mypy override для `factory.*`)

---

## Task 1: Апка products — моделі, менеджер, admin, factories

**Files:**
- Create: `products/__init__.py`, `products/apps.py`, `products/models.py`, `products/admin.py`, `products/migrations/__init__.py`, `products/tests/__init__.py`, `products/tests/factories.py`, `products/tests/test_models.py`
- Modify: `config/settings/base.py` (INSTALLED_APPS), `pyproject.toml` (mypy override)

- [ ] **Step 1: Каркас апки + реєстрація**

`products/__init__.py`, `products/migrations/__init__.py`, `products/tests/__init__.py` — порожні.

`products/apps.py`:
```python
"""Конфігурація застосунку products."""
from django.apps import AppConfig


class ProductsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "products"
```

`config/settings/base.py` — додати `"products",` у `INSTALLED_APPS` (після `"users",`).

`pyproject.toml` — додати mypy-оверайд (factory_boy без стабів):
```toml
[[tool.mypy.overrides]]
module = ["factory.*"]
ignore_missing_imports = true
```

- [ ] **Step 2: Написати падаючі тести моделей**

`products/tests/test_models.py`:
```python
"""Тести моделей каталогу."""
from decimal import Decimal

import pytest
from django.core.exceptions import ValidationError

from products.models import Category, Product


@pytest.mark.django_db
def test_category_slug_autogenerated() -> None:
    cat = Category.objects.create(name="Hops")
    assert cat.slug == "hops"


@pytest.mark.django_db
def test_product_slug_autogenerated() -> None:
    cat = Category.objects.create(name="Hops")
    product = Product.objects.create(name="Cascade Hops", price=Decimal("5.50"), category=cat)
    assert product.slug == "cascade-hops"


@pytest.mark.django_db
def test_product_str_is_name() -> None:
    cat = Category.objects.create(name="Hops")
    product = Product.objects.create(name="Cascade Hops", price=Decimal("5.50"), category=cat)
    assert str(product) == "Cascade Hops"


@pytest.mark.django_db
def test_product_price_cannot_be_negative() -> None:
    cat = Category.objects.create(name="Hops")
    product = Product(name="Bad", price=Decimal("-1"), category=cat)
    with pytest.raises(ValidationError):
        product.full_clean()


@pytest.mark.django_db
def test_active_manager_excludes_inactive() -> None:
    cat = Category.objects.create(name="Hops")
    Product.objects.create(name="A", price=Decimal("1"), category=cat, is_active=True)
    Product.objects.create(name="B", price=Decimal("2"), category=cat, is_active=False)
    assert Product.objects.active().count() == 1
```

- [ ] **Step 3: Запустити — переконатись, що падає**

Run: `uv run pytest products/tests/test_models.py -q`
Expected: FAIL (ModuleNotFoundError: products.models / no Category).

- [ ] **Step 4: Реалізувати моделі**

`products/models.py`:
```python
"""Моделі каталогу: категорії та товари."""
from typing import Any

from django.core.validators import MinValueValidator
from django.db import models
from django.urls import reverse
from django.utils.text import slugify


class Category(models.Model):
    name = models.CharField(max_length=100)
    slug = models.SlugField(max_length=120, unique=True, blank=True)
    parent = models.ForeignKey(
        "self", null=True, blank=True, on_delete=models.SET_NULL, related_name="children"
    )
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        ordering = ["name"]
        verbose_name_plural = "categories"

    def __str__(self) -> str:
        return self.name

    def save(self, *args: Any, **kwargs: Any) -> None:
        if not self.slug:
            self.slug = slugify(self.name)
        super().save(*args, **kwargs)


class ProductQuerySet(models.QuerySet["Product"]):
    def active(self) -> "ProductQuerySet":
        return self.filter(is_active=True)


class Product(models.Model):
    name = models.CharField(max_length=200)
    slug = models.SlugField(max_length=220, unique=True, blank=True)
    description = models.TextField(blank=True)
    price = models.DecimalField(
        max_digits=10, decimal_places=2, validators=[MinValueValidator(0)]
    )
    category = models.ForeignKey(Category, on_delete=models.PROTECT, related_name="products")
    image = models.ImageField(upload_to="products/", blank=True)
    is_active = models.BooleanField(default=True)
    stock = models.PositiveIntegerField(default=0)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    objects = ProductQuerySet.as_manager()

    class Meta:
        ordering = ["-created_at"]

    def __str__(self) -> str:
        return self.name

    def save(self, *args: Any, **kwargs: Any) -> None:
        if not self.slug:
            self.slug = slugify(self.name)
        super().save(*args, **kwargs)

    def get_absolute_url(self) -> str:
        return reverse("products:detail", kwargs={"slug": self.slug})
```

- [ ] **Step 5: Міграції + прогнати тести**

Run: `uv run python manage.py makemigrations products && uv run pytest products/tests/test_models.py -q`
Expected: міграція `0001_initial` створена; 5 passed.

- [ ] **Step 6: Factories**

`products/tests/factories.py`:
```python
"""factory_boy-фабрики для тестів каталогу."""
from decimal import Decimal

import factory
from factory.django import DjangoModelFactory

from products.models import Category, Product


class CategoryFactory(DjangoModelFactory[Category]):
    class Meta:
        model = Category

    name = factory.Sequence(lambda n: f"Category {n}")


class ProductFactory(DjangoModelFactory[Product]):
    class Meta:
        model = Product

    name = factory.Sequence(lambda n: f"Product {n}")
    description = "Test product"
    price = Decimal("9.99")
    category = factory.SubFactory(CategoryFactory)
    stock = 10
    is_active = True
```

- [ ] **Step 7: Admin**

`products/admin.py`:
```python
"""Адмінка каталогу."""
from django.contrib import admin

from .models import Category, Product


@admin.register(Category)
class CategoryAdmin(admin.ModelAdmin[Category]):
    list_display = ("name", "slug", "parent")
    search_fields = ("name",)
    prepopulated_fields = {"slug": ("name",)}


@admin.register(Product)
class ProductAdmin(admin.ModelAdmin[Product]):
    list_display = ("name", "category", "price", "stock", "is_active")
    list_filter = ("category", "is_active")
    search_fields = ("name", "description")
    prepopulated_fields = {"slug": ("name",)}
    list_editable = ("price", "stock", "is_active")
```

- [ ] **Step 8: Лінт/типи + коміт**

Run: `uv run ruff check . && uv run mypy . && uv run pytest -q`
Expected: clean; усі тести passed.
```bash
git add products config/settings/base.py pyproject.toml
git commit -m "feat(products): моделі Category/Product, менеджер, admin, factories"
```

---

## Task 2: Команда seed_catalog

**Files:**
- Create: `products/management/__init__.py`, `products/management/commands/__init__.py`, `products/management/commands/seed_catalog.py`, `products/tests/test_seed.py`

- [ ] **Step 1: Написати падаючий тест**

`products/tests/test_seed.py`:
```python
"""Тест seed-команди каталогу."""
import pytest
from django.core.management import call_command

from products.models import Category, Product


@pytest.mark.django_db
def test_seed_creates_catalog() -> None:
    call_command("seed_catalog")
    assert Category.objects.count() == 4
    assert Product.objects.count() == 12


@pytest.mark.django_db
def test_seed_is_idempotent() -> None:
    call_command("seed_catalog")
    call_command("seed_catalog")
    assert Category.objects.count() == 4
    assert Product.objects.count() == 12
```

- [ ] **Step 2: Запустити — переконатись, що падає**

Run: `uv run pytest products/tests/test_seed.py -q`
Expected: FAIL (Unknown command 'seed_catalog').

- [ ] **Step 3: Реалізувати команду**

`products/management/__init__.py`, `products/management/commands/__init__.py` — порожні.

`products/management/commands/seed_catalog.py`:
```python
"""Наповнення каталогу демо-даними зі статичного шаблону Hop & Barley."""
from decimal import Decimal
from typing import Any

from django.core.management.base import BaseCommand

from products.models import Category, Product

CATEGORIES = ["Hops", "Malts", "Yeast", "Kits"]

# (name, category, price, stock)
PRODUCTS: list[tuple[str, str, str, int]] = [
    ("Cascade Hops", "Hops", "4.50", 100),
    ("Centennial Hops", "Hops", "5.00", 80),
    ("Citra Hops", "Hops", "6.50", 60),
    ("Mosaic Hops", "Hops", "6.75", 50),
    ("Saaz Hops", "Hops", "5.25", 70),
    ("Maris Otter Malt", "Malts", "2.20", 200),
    ("Pilsner Malt", "Malts", "2.00", 200),
    ("Caramel Malt", "Malts", "2.50", 150),
    ("Unmalted Wheat", "Malts", "1.90", 180),
    ("Safale US-05 Yeast", "Yeast", "3.80", 120),
    ("Imperial Yeast", "Yeast", "8.00", 40),
    ("West Coast IPA Kit", "Kits", "39.99", 25),
]


class Command(BaseCommand):
    help = "Створює демо-категорії та товари (ідемпотентно)."

    def handle(self, *args: Any, **options: Any) -> None:
        cats: dict[str, Category] = {}
        for name in CATEGORIES:
            cats[name], _ = Category.objects.get_or_create(name=name)
        for name, cat_name, price, stock in PRODUCTS:
            Product.objects.get_or_create(
                name=name,
                defaults={
                    "category": cats[cat_name],
                    "price": Decimal(price),
                    "stock": stock,
                    "description": f"{name} — демо-товар.",
                },
            )
        self.stdout.write(self.style.SUCCESS("Каталог наповнено."))
```

- [ ] **Step 4: Прогнати тест**

Run: `uv run pytest products/tests/test_seed.py -q`
Expected: 2 passed.

- [ ] **Step 5: Лінт/типи + коміт**

Run: `uv run ruff check . && uv run mypy .`
```bash
git add products/management products/tests/test_seed.py
git commit -m "feat(products): команда seed_catalog (демо-дані, ідемпотентна)"
```

---

## Task 3: Каталог-лист (view, url, base.html, шаблон, статика)

**Files:**
- Create: `products/views.py`, `products/urls.py`, `products/templates/products/product_list.html`
- Modify: `config/urls.py`, `templates/base.html`, `static/`
- Test: `products/tests/test_views.py`

- [ ] **Step 1: Написати падаючі тести каталогу**

`products/tests/test_views.py`:
```python
"""Тести в'юх каталогу."""
from decimal import Decimal
from typing import Any

import pytest
from django.test import Client, RequestFactory
from django.urls import reverse

from products.models import Product
from products.tests.factories import CategoryFactory, ProductFactory
from products.views import ProductListView


@pytest.mark.django_db
def test_catalog_lists_only_active(client: Client) -> None:
    ProductFactory(name="Visible", is_active=True)
    ProductFactory(name="Hidden", is_active=False)
    resp = client.get(reverse("products:list"))
    assert resp.status_code == 200
    names = [p.name for p in resp.context["products"]]
    assert "Visible" in names
    assert "Hidden" not in names


@pytest.mark.django_db
def test_catalog_pagination(client: Client) -> None:
    ProductFactory.create_batch(15)
    resp = client.get(reverse("products:list"))
    assert len(resp.context["products"]) == 12


@pytest.mark.django_db
def test_catalog_filter_by_category(client: Client) -> None:
    cat = CategoryFactory(name="Hops")
    other = CategoryFactory(name="Malts")
    ProductFactory(name="InHops", category=cat)
    ProductFactory(name="InMalts", category=other)
    resp = client.get(reverse("products:list"), {"category": cat.slug})
    names = [p.name for p in resp.context["products"]]
    assert names == ["InHops"]


@pytest.mark.django_db
def test_catalog_filter_by_price_range(client: Client) -> None:
    ProductFactory(name="Cheap", price=Decimal("5"))
    ProductFactory(name="Pricey", price=Decimal("50"))
    resp = client.get(reverse("products:list"), {"min_price": "10", "max_price": "100"})
    names = [p.name for p in resp.context["products"]]
    assert names == ["Pricey"]


@pytest.mark.django_db
def test_catalog_search_name_and_description(client: Client) -> None:
    ProductFactory(name="Cascade Hops", description="citrus aroma")
    ProductFactory(name="Pilsner Malt", description="base malt")
    by_name = client.get(reverse("products:list"), {"q": "cascade"})
    assert [p.name for p in by_name.context["products"]] == ["Cascade Hops"]
    by_desc = client.get(reverse("products:list"), {"q": "base malt"})
    assert [p.name for p in by_desc.context["products"]] == ["Pilsner Malt"]


@pytest.mark.django_db
def test_catalog_sort_by_price(client: Client) -> None:
    ProductFactory(name="B", price=Decimal("20"))
    ProductFactory(name="A", price=Decimal("10"))
    resp = client.get(reverse("products:list"), {"sort": "price"})
    prices = [p.price for p in resp.context["products"]]
    assert prices == sorted(prices)


@pytest.mark.django_db
def test_catalog_queryset_avoids_n_plus_one(django_assert_num_queries: Any) -> None:
    cat = CategoryFactory()
    ProductFactory.create_batch(3, category=cat)
    view = ProductListView()
    view.request = RequestFactory().get("/")
    with django_assert_num_queries(1):
        products = list(view.get_queryset())
        _ = [p.category.name for p in products]
    assert len(products) == 3
```

- [ ] **Step 2: Запустити — переконатись, що падає**

Run: `uv run pytest products/tests/test_views.py -q`
Expected: FAIL (cannot import ProductListView / no url 'products:list').

- [ ] **Step 3: View + safe-parse**

`products/views.py`:
```python
"""В'юхи каталогу."""
from decimal import Decimal, InvalidOperation
from typing import Any

from django.db.models import Q, QuerySet
from django.views.generic import DetailView, ListView

from .models import Category, Product

SORT_OPTIONS = {"price", "-price", "-created_at"}


def _parse_decimal(value: str | None) -> Decimal | None:
    if not value:
        return None
    try:
        return Decimal(value)
    except (InvalidOperation, TypeError):
        return None


class ProductListView(ListView):
    model = Product
    template_name = "products/product_list.html"
    context_object_name = "products"
    paginate_by = 12

    def get_queryset(self) -> QuerySet[Product]:
        qs = Product.objects.active().select_related("category")
        params = self.request.GET
        if category := params.get("category"):
            qs = qs.filter(category__slug=category)
        if (min_price := _parse_decimal(params.get("min_price"))) is not None:
            qs = qs.filter(price__gte=min_price)
        if (max_price := _parse_decimal(params.get("max_price"))) is not None:
            qs = qs.filter(price__lte=max_price)
        if q := params.get("q"):
            qs = qs.filter(Q(name__icontains=q) | Q(description__icontains=q))
        sort = params.get("sort")
        if sort in SORT_OPTIONS:
            qs = qs.order_by(sort)
        return qs

    def get_context_data(self, **kwargs: Any) -> dict[str, Any]:
        ctx = super().get_context_data(**kwargs)
        ctx["categories"] = Category.objects.all()
        ctx["current"] = self.request.GET
        return ctx


class ProductDetailView(DetailView):
    model = Product
    template_name = "products/product_detail.html"
    context_object_name = "product"

    def get_queryset(self) -> QuerySet[Product]:
        return Product.objects.active().select_related("category")
```

- [ ] **Step 4: URLs**

`products/urls.py`:
```python
"""URL-и каталогу."""
from django.urls import path

from . import views

app_name = "products"

urlpatterns = [
    path("", views.ProductListView.as_view(), name="list"),
    path("products/", views.ProductListView.as_view(), name="list_alt"),
    path("product/<slug:slug>/", views.ProductDetailView.as_view(), name="detail"),
]
```

`config/urls.py` — додати у `urlpatterns` (перед статикою/після admin):
```python
    path("", include("products.urls")),
```
та оновити імпорт: `from django.urls import URLPattern, URLResolver, include, path`.

- [ ] **Step 5: base.html + product_list.html + статика**

1. Скопіювати статику зі склонованого шаблону:
```bash
cp /tmp/hop-and-barley/myshop/static/css/main.css static/css/main.css
cp -r /tmp/hop-and-barley/myshop/static/img static/img
cp -r /tmp/hop-and-barley/myshop/static/js static/js
```
(створи `static/css/` за потреби; видали JS, що робить клієнтську фільтрацію — фільтр серверний.)

2. Переписати `templates/base.html`: взяти `<!DOCTYPE>…<head>` + спільний хедер/нав + футер із `/tmp/hop-and-barley/myshop/templates/home.html`; замість inline-стилів підключити `{% load static %}` → `<link rel="stylesheet" href="{% static 'css/main.css' %}">`; контент-зону обернути в `{% block content %}{% endblock %}`; нав-лінки на cart/account/login лишити `href="#"` (замінимо в їхніх юнітах); посилання на каталог — `{% url 'products:list' %}`.

3. `products/templates/products/product_list.html` — `{% extends "base.html" %}`; портувати грід товарів із `home.html`, але замість статичних карток:
```django
{% extends "base.html" %}
{% load static %}
{% block content %}
<form method="get">
  <input type="search" name="q" value="{{ current.q }}" placeholder="Пошук...">
  <select name="category">
    <option value="">Усі категорії</option>
    {% for c in categories %}
      <option value="{{ c.slug }}" {% if current.category == c.slug %}selected{% endif %}>{{ c.name }}</option>
    {% endfor %}
  </select>
  <input type="number" name="min_price" value="{{ current.min_price }}" placeholder="від">
  <input type="number" name="max_price" value="{{ current.max_price }}" placeholder="до">
  <select name="sort">
    <option value="">За новизною</option>
    <option value="price" {% if current.sort == 'price' %}selected{% endif %}>Ціна ↑</option>
    <option value="-price" {% if current.sort == '-price' %}selected{% endif %}>Ціна ↓</option>
  </select>
  <button type="submit">Застосувати</button>
</form>

<div class="product-grid">
  {% for product in products %}
    <a class="product-card" href="{{ product.get_absolute_url }}">
      {% if product.image %}
        <img src="{{ product.image.url }}" alt="{{ product.name }}">
      {% else %}
        <img src="{% static 'img/placeholder.png' %}" alt="{{ product.name }}">
      {% endif %}
      <h3>{{ product.name }}</h3>
      <p>{{ product.category.name }}</p>
      <strong>${{ product.price }}</strong>
    </a>
  {% empty %}
    <p>Нічого не знайдено.</p>
  {% endfor %}
</div>

{% if is_paginated %}
<nav class="pagination">
  {% if page_obj.has_previous %}<a href="?{% if current.q %}q={{ current.q }}&{% endif %}page={{ page_obj.previous_page_number }}">←</a>{% endif %}
  <span>{{ page_obj.number }} / {{ page_obj.paginator.num_pages }}</span>
  {% if page_obj.has_next %}<a href="?page={{ page_obj.next_page_number }}">→</a>{% endif %}
</nav>
{% endif %}
{% endblock %}
```
(Класи підлаштувати під `main.css`. Якщо в `static/img` немає `placeholder.png` — додати будь-яке зображення-заглушку або взяти наявне з шаблону.)

- [ ] **Step 6: Прогнати тести каталогу**

Run: `uv run pytest products/tests/test_views.py -q`
Expected: 7 passed. (Якщо `product.image.url` падає на порожньому — гілка `{% else %}` має це закривати; перевір умову `{% if product.image %}`.)

- [ ] **Step 7: Ручна перевірка + лінт/типи + коміт**

```bash
uv run python manage.py seed_catalog
uv run python manage.py runserver  # відкрий http://127.0.0.1:8000/ — каталог із товарами
```
Run: `uv run ruff check . && uv run mypy . && uv run pytest -q`
```bash
git add products config/urls.py templates/base.html static
git commit -m "feat(products): каталог-лист (фільтри/пошук/сорт/пагінація) + base.html + статика"
```

---

## Task 4: Детальна сторінка товару

**Files:**
- Create: `products/templates/products/product_detail.html`
- Test: `products/tests/test_views.py` (доповнити)

- [ ] **Step 1: Дописати падаючі тести детальної**

Додати в `products/tests/test_views.py`:
```python
@pytest.mark.django_db
def test_detail_active_product_200(client: Client) -> None:
    product = ProductFactory(name="Cascade Hops", is_active=True)
    resp = client.get(product.get_absolute_url())
    assert resp.status_code == 200
    assert resp.context["product"] == product


@pytest.mark.django_db
def test_detail_inactive_product_404(client: Client) -> None:
    product = ProductFactory(is_active=False)
    resp = client.get(product.get_absolute_url())
    assert resp.status_code == 404


@pytest.mark.django_db
def test_detail_missing_slug_404(client: Client) -> None:
    resp = client.get(reverse("products:detail", kwargs={"slug": "nope"}))
    assert resp.status_code == 404
```

- [ ] **Step 2: Запустити — переконатись, що падає**

Run: `uv run pytest products/tests/test_views.py -k detail -q`
Expected: FAIL (TemplateDoesNotExist: products/product_detail.html).

- [ ] **Step 3: Шаблон детальної**

`products/templates/products/product_detail.html` — `{% extends "base.html" %}`, портувати розмітку з будь-якої `product-*.html`, поля динамічні + плейсхолдери:
```django
{% extends "base.html" %}
{% load static %}
{% block title %}{{ product.name }}{% endblock %}
{% block content %}
<div class="product-detail">
  {% if product.image %}
    <img src="{{ product.image.url }}" alt="{{ product.name }}">
  {% else %}
    <img src="{% static 'img/placeholder.png' %}" alt="{{ product.name }}">
  {% endif %}
  <h1>{{ product.name }}</h1>
  <p class="category">{{ product.category.name }}</p>
  <strong class="price">${{ product.price }}</strong>
  <div class="description">{{ product.description|linebreaks }}</div>

  {# TODO(cart unit): кнопка «додати в кошик» — поки неактивна #}
  <button type="button" disabled>Додати в кошик</button>

  {# TODO(reviews unit): секція відгуків з'явиться тут #}
  <section class="reviews"><h2>Відгуки</h2><p>Скоро.</p></section>
</div>
{% endblock %}
```

- [ ] **Step 4: Прогнати тести**

Run: `uv run pytest products/tests/test_views.py -k detail -q`
Expected: 3 passed.

- [ ] **Step 5: Уся перевірка + коміт**

Run: `uv run ruff check . && uv run mypy . && uv run pytest -q`
Expected: усе зелене (моделі + seed + каталог + детальна).
```bash
git add products
git commit -m "feat(products): детальна сторінка товару (інфо + плейсхолдери reviews/cart)"
```

---

## Completion

Після Task 4: `superpowers:requesting-code-review` → правки → `superpowers:finishing-a-development-branch` (merge `feature/catalog` → `develop`).
